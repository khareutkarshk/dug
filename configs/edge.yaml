server:
  port: 8080
  retries: 2

  rate_limit:
    rps: 10
    burst: 20

routes:
  - path: /
    strategy: smooth_weighted

    upstreams:
  - url: http://localhost:3001
    weight: 5

  - url: http://localhost:3002
    weight: 3